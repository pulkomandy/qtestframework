CONFIG += c++11 release

# Look for includes in the includes dir
INCLUDEPATH += ../Includes
VPATH += ../Includes

# Store all generated files in the Build directory, don't mess up the source dirs.
OBJECTS_DIR = ../Build
MOC_DIR = ../Build

